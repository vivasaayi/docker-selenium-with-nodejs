const assert = require('assert');

describe('webdriver.io page', () => {
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
  it('should have the right title', () => {
    browser.url('http://poriyiyal.azurewebsites.net/');
    const title = browser.getTitle();
    assert.equal(title, 'WebdriverIO · Next-gen WebDriver test framework for Node.js');
    browser.pause(3000)
  });
});